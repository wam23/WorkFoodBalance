<% provide(:title, 'Anmeldung') %>

<h2>Falls ihr bereits ein Login habt, meldet euch bitte <%= link_to "-> hier <-", login_path %> an!</h2>
<h1>Anmeldung</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@user) do |f| %>
        <%= render 'fields', f: f %>
            
            <%= f.label :password, "Passwort" %>
            <%= f.password_field :password, class: 'form-control' %>

            <%= f.label :password_confirmation, "Passwort bestätigen" %>
            <%= f.password_field :password_confirmation, class: 'form-control' %>
        
        <%= recaptcha_tags %>

        <%= f.submit "Anmelden", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>
